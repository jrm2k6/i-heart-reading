import React from 'react'

export default class BookComponent extends React.Component {
    render() {
        return (
            <div>
              Add Book component
            </div>
        );
    }
}
