import {
  UPLOAD_SUCCESS,
  UPLOAD_PROGRESS,
  UPLOAD_FAILED
} from '../actions/uploadActions';

const initialState = {
  uploads: []
};

export default function searchReducer(state = initialState, action) {
  return state;
}
